# middleware/

Lightweight Clean Architecture — plain Node, zero deps.

```text
adapters/  →  use-cases/  →  domain/
data/      →  (ports used by use-cases)
```

See [FOUNDATION.md](../FOUNDATION.md).

```bash
npm start
npm test
```
